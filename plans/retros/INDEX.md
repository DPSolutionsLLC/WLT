# Retro Context Index

Quick-reference for the retrolearn command. One line per entry.

## Features

- [foundation-a-scaffold](foundation-a-scaffold.md) — Next.js 16 + Supabase skeleton, three client factories, class-based dark mode, Vitest runner (c5ca596)
- [foundation-b-schema](foundation-b-schema.md) — 19 migrations, 49 tables, RLS on all of them, two restricted public views, seed data, generated types (78ef8fe)
- [foundation-c-services](foundation-c-services.md) — scopedQuery, permission matrix, writeAuditLog, emitNotification, six RLS suites, 108 tests (5ccf83a)
- [auth-a-session-shell](auth-a-session-shell.md) — session resolution, adult sign-in, deactivation enforcement, middleware guard, role-filtered app shell, ward-scoped users read (e403c51)
- [auth-b-invites-admin](auth-b-invites-admin.md) — role-bearing invite links, atomic single-use redemption, registration page, admin user management, last-bishop lockout guard (8f10ec1)
- [auth-c-youth-pin](auth-c-youth-pin.md) — youth username+PIN accounts, synthetic .invalid auth addresses, 5-failure lockout, separate youth shell, `pin_hash` dropped (e8f3a66)
- [roster-a-data-and-pages](roster-a-data-and-pages.md) — roster data layer with an active-only default, browse and detail pages, household/member CRUD, bishopric-only member notes, `apply_roster_import`, `users` column grant closed (f185c7d)
- [roster-b-picker-and-orgs](roster-b-picker-and-orgs.md) — frozen controlled `MemberPicker`, native-dialog `Modal`, TanStack Query provider, organization membership with per-member and bulk assign, org-scoped roster default (d6cb4da)
- [roster-c-csv-import](roster-c-csv-import.md) — LCR CSV import wizard, dependency-free RFC 4180 parser, no-write preview, single-transaction apply; scenario 009 walked, preview/result count mismatch and file-changed error message fixed, alias table still unverified against a real export (6d405a6)
- [deployment](deployment.md) — live on Vercel at wlt-iota.vercel.app, Node 22 pinned, previews disabled in `vercel.json`, Supabase auth URLs and Resend SMTP configured, password reset verified end to end; a build with no env vars still exits 0 (0a4c33a, 1febf89)
- [calendar-a-rules-and-api](calendar-a-rules-and-api.md) — UTC date-only helpers, Fast Sunday as a re-running resolution rule with a 409 confirm path, versioned conducting rotation, ward-configurable default speaker count; select-column concatenation and a shape-only date schema both fixed (6d5048f)
- [calendar-b-month-view](calendar-b-month-view.md) — month grid and 375px card list, Sunday detail and editor, the 409 confirm dialog worded by consequence, nine pipeline-stage tokens and real reserved-region props for Phase 4; scenario 010 walked with no code defects, but surfaced that the rotation should be monthly (b4b721d)
- [calendar-c-rotation-cadence](calendar-c-rotation-cadence.md) — weekly/monthly rotation cadence, independent rotations for the six organizations, per-Sunday org conducting, and the first org-scoped write boundary in the app (RLS, not just the route); half-generated months found and made self-repairing; scenario 011 written but not yet walked (13fced6)
- [talks-a-pipeline-core](talks-a-pipeline-core.md) — nine-stage pipeline as a pure state machine, 3-of-3 approval gate with a DB uniqueness constraint behind it, ITER-004 external speakers with an explicit contact waiver, four assignment routes, and the `assignment_reverted` notification calendar-b handed forward (a260ca6)
- [talks-b-month-planner](talks-b-month-planner.md) — month planner, assignment modal and Sunday detail page, n-of-3 approvals read as a sentence with names, ITER-004 waived contact stages that read as "Not applicable" with no disabled controls, sms: handoff with a copy fallback, realtime comment threads, calendar-b's two reserved regions filled; stage token contrast measured in both themes and two retuned; scenarios 012 and 013 walked (036698c)
- [route-tests-and-realtime](route-tests-and-realtime.md) — route handlers made testable with a one-module mock (65 tests over the four assignment routes), the first `supabase_realtime` publication entry with a cross-ward leak test gating it, scenarios 012/013 trimmed to what needs a human; corrects six retros' "there is no local server", a timeout-based privacy test that passed while realtime was dead, and a shared-channel-topic bug in `CommentThread` that the missing publication had been masking (0db037f)
- [sunday-types-meeting-split](sunday-types-meeting-split.md) — ITER-002/003: split `FAST_SUNDAY_DISPLACING_TYPES` into "cannot be Fast Sunday" and "holds no meeting", added `ward_conference`, made the conducting rotation skip cancelled Sundays for both cadences and re-shift forward behind the confirm dialog; a CHECK constraint makes a conductor-without-a-meeting unrepresentable, and a latent slot-restore bug reachable since Phase 3A was found and fixed (0e4960a)
- [talks-c-prayers-topics](talks-c-prayers-topics.md) — four-stage prayer pipeline with a last-prayed nudge that renders NOTHING rather than "Never", the topic library with staleness ordering, and the AI accept/reject queue built and shipped empty before Phase 5 can fill it; `last_assigned_at` stamped at `approve` only and proven to survive a revert; talks-b's two handed-forward gaps closed; scenarios 016/017 walked in a browser (429f5f3)
- [talks-d-reliability-goals](talks-d-reliability-goals.md) — four boundary-tested reliability flags on a bishopric-only speaker profile behind its own route, the goals board with status computed on read, and Phase 4 closed; walking it moved goal alerts off the calendar onto the Sunday planner and closed the goals org-scoping asymmetry that Phase 11 was going to inherit (0fe6564)
- [ai-a-client-and-settings](ai-a-client-and-settings.md) — the first Claude call: typed client with six distinct error kinds, a pure system-prompt assembler with the cache breakpoint on the last stable block, and the append-only AI settings panel whose preview runs against unsaved form state; walking it found that `router.refresh()` preserves client state, so restoring a version left the form stale while every server test passed (4dee9a2)
- [ai-b-knowledge-and-retrieval](ai-b-knowledge-and-retrieval.md) — layer 3 on: chunking, OpenAI embeddings, `match_document_chunks` behind an HNSW index (a deliberate deviation from the plan's ivfflat, which removes the build-it-afterwards step), document management UI with a Retrieval Tester, and a standard-works ingest script that runs under plain Node; a walk interrupted mid-step was resumed from the audit log rather than re-seeded, and found a "all 1 of its passages" plural bug that the fixture's own design had hidden, plus three checklist items that were wrong or could not fail (96b25df)
- [ai-c-feature-routes](ai-c-feature-routes.md) — Phase 5 spent: topic suggestions into the accept/reject queue and message drafts into the two existing textareas, both writing nothing a person did not approve; the thank-you finally receives the comment thread `talks-b` left hard-coded to `[]`; scenarios 024 and 025 walked, which found four defects (one new), two confirmed-wrong AI citations that became ITER-016, and two checklist items describing states the app cannot reach (882ef04)
- [ai-d-corpus-scoping](ai-d-corpus-scoping.md) — ITER-011 closed and ITER-012's logging half: conference metadata, a filtered `match_document_chunks` that exempts the standard works, the scope panel with a live count, and a phrase-to-filter resolver that saves nothing until accepted; an empty-array CHECK shipped inert because `array_length` returns NULL on `'{}'` (d96b83d)

- [program-a-draft-and-approval](program-a-draft-and-approval.md) — the program draft as a SNAPSHOT rather than a view, proven by changing an assignment underneath a stored one; a printed/public name pair on every person computed once at assembly so program-c's projector cannot reach a surname; four routes with a discriminated action; `programs` writes narrowed below 019's ward-wide loop; and migration 038, which found that `talks.view` had been dead at the database since foundation-c — a ward secretary's program assembled with every speaking slot silently empty and answered 200 (91f0f75)
- [program-b-builder-screen](program-b-builder-screen.md) — the builder screen over program-a's snapshot: a missing-work panel written as sentences rather than field names, one diff panel shared by the refresh flow and the AI editor, and an ai-edit route that returns a proposed draft and writes nothing; walking scenario 031 found that OMITTING an empty meeting-order line reads as "failed to load" rather than as an absence, that the screen claimed a notification it could not know was sent, and — via the regression test for that — a cache write racing a refetch already in flight (b8470ab)
- [program-c-public-pages](program-c-public-pages.md) — the unauthenticated read surface: a narrow `public_data` projection computed by one pure function at approval, migration 039's view exposing that column and nothing else, and a no-chrome `/public/[slug]` reading it through an anon client that holds no grant on any base table; the privacy test scans the output for strings that must not appear rather than listing fields that may. Walking it REVERSED two of the plan's own decisions — names are published in full (shortening only the members read as a bug beside the visitor's full name) and an empty slot renders rather than vanishing (a slot that disappears looks correct, so nobody fixes the slot count) — and found that `program-b` told people to reopen an approved program while giving them no button to do it (07709ed)
- [program-d-pdf-and-distribution](program-d-pdf-and-distribution.md) — the bifold PDF, a private storage bucket and the first Resend call: four panels imposed onto one landscape sheet (panel order is not reading order, and only paper can check it), a QR encoding the public URL, and the `public_pages` row that makes `program-c` reachable for the first time. Email ships OFF until a domain is verified, so distribution PUBLISHES and says plainly that it did not send. Found and fixed a `revalidatePath` throw that reported "nothing was marked as sent" AFTER an irreversible send, a declared-but-never-thrown `pdf_missing` kind, and a notice that claimed nothing was emailed when it did not know. ITER-004 closed. **Scenarios 034/035 not yet walked — M4 waits on the fold check** (be4ea6e)
- [program-e-music-and-hymns](program-e-music-and-hymns.md) — Phase 6 closed: the coordinator's screen, hymn search, and AI suggestions that RANK a candidate list rather than recall a number, every returned number checked against the table before it reaches a screen. Migration 042's `source` column let the 299 missing hymns be filled with rows nobody could mistake for real ones, so the seed file's "do not pad this with plausible entries" rule was honoured rather than waived; `gather.ts` handed its two temporary readers to `lib/music/queries.ts` as `program-a` planned. Walking scenario 036 verified three suggested numbers against a PHYSICAL HYMNBOOK — the first AI output here checked against a source outside the app's own data — and found that `writeAuditLog` has been redacting its own token counts since `ai-c`, plus that a person who confirms a hymn by hand has nowhere to record it (a329459)
- [visits-a-goals-logs-and-notes](visits-a-goals-logs-and-notes.md) — visit goals, visit logs and the private-notes boundary proven at the ROUTE layer (the RLS half had passed since Phase 0 and would have kept passing through a widened select); flag notification narrowed to the executive secretary by migration 045 rather than a hand-edited row; walking it found a moved-out household offered as somewhere to visit and moved the notes emphasis off the private field onto the shared one, where the caution actually belongs (d6b8e6f)
- [visits-d-attempts-appointments-and-participants](visits-d-attempts-appointments-and-participants.md) — attempted vs completed visits, appointment vs drop-in, a `visit_appointments` table where "missed" is computed on read, and `visited_by` split into `recorded_by` plus a `visit_participants` table that can name a user, a member or a typed name; the RLS suite caught a composite `on delete set null` that made visits UNDELETABLE and a parent-scope hole that let one org write a participant onto another's visit, and walking it found the "Log this visit" flow completely dead because a constant imported from a `"use client"` module reaches a Server Component as a function, not a string (842968d)

## Bug Fixes

- [role-access-overrides](role-access-overrides.md) — 25 of 62 permission checks ignored `wards.settings.role_access` because `can()`'s third parameter defaulted to the code matrix; default dropped so the compiler enumerated them, `admin.*`/`sacrament.*` locked in both directions, stored shape changed to add/remove deltas, bishopric equivalence enforced in `mergeRoleAccess` (310a708) → fixes: foundation-c-services
- [seed-household-id-collision](seed-household-id-collision.md) — scenario 008 had never seeded: `createHousehold` keyed its id on the family name alone, so two "Smith" households collided on `households_pkey` and the seed died after 3 households and 0 members (2af66b8)
