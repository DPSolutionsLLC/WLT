# Pending Confirmations

Items awaiting testing/confirmation. Oldest items should be tested first.

**"Deployed build unopened" was closed on 2026-08-29** by `deployed-build-2026-08-29.md`. It stood
open in the four rows below since 2026-08-21. The older rows are left as written — they are the
record of what was true when each was made — but that particular item is no longer outstanding for
any of them. What the first look found is the new `deployed-build` row.

| Date | Area | Commit | Status | Notes |
|------|------|--------|--------|-------|
| 2026-08-21 | talks-planner | 0db037f | best-yet | Scenario 012 re-walked incl. the two-browser realtime check. Closed since: realtime, the Failure Behavior checks, and scenario-008 (walked 2026-08-22, now `confirmed` under roster-picker). Still open: deployed build unopened, `sms:` untested on a real device, scenario 013 not re-walked since trimming. Supersedes the 036698c record |
| 2026-08-26 | visits | 842968d | best-yet | Scenarios 043 and 044 walked by an AGENT on localhost; user reviewed screenshots. Four defects found that 2304 tests missed, three fixed and re-walked. Still open: **D3 is an open decision, not a fix** — the companion picker is org-scoped, so a member from another org has never been addable; the picker also reports an empty roster for a filtered-empty result. Also unverified: the deployed build, a real device, reschedule through the UI, and the bishopric path. Closed since: migration 049 applied 2026-08-26, and the missing year on appointment dates fixed in `visits-b` (d5695e7) |
| 2026-08-26 | visits-report-feed | 16b7c03 | best-yet | Scenarios 041 and 042 walked by an AGENT in a browser; user reviewed screenshots and answered seven judgement questions. Three defects found and fixed after 2421 tests missed all three: `/visits` offered "Flag for ward council" on other organizations' visits once cross-org visibility went on (RLS refused them, nothing leaked), the filter listed five organizations that answer with an empty feed, and a bookmark made under one filter was invisible under another until reload. Still open: **deployed build unopened**, no real device, dark mode reviewed only via screenshots, pagination never exercised (every fixture fits one page), Mark-all-under-filter not re-walked after the cache fix, and `youth_activity` read-status proven by route test only because Phase 8 has no screen. Separate area from the `visits` row above on purpose — that record's D3 decision is untouched by this work and must not be superseded |
| 2026-08-28 | youth-ics-import | 45dd928 | best-yet | Scenarios 051 and 052 walked by an AGENT in a browser against the hosted project; user reviewed the walk report and answered five judgement questions. **Record written 2026-08-30 from the walk evidence, not from a fresh walk** — this slice, `youth-coverage-calendar` and `youth-followup-feed` were the only walked slices in the project with no confirmation record, and `plans/INDEX.md` described all three as unwalked until the same day. That stale line sent a later session off to re-walk work already done, which is why the gap is recorded rather than quietly closed. Every value read back with the service client; row ids captured **before** the re-import so "updated, not replaced" was proved rather than assumed. All seven ICS code paths land at the published hour; the preview writes nothing (0 rows in four tables, re-read while it was on screen); counts agree 12/0/0/0 end to end. **Migration 055's unique index proved DIRECTLY** — the two concurrent confirms serialised and never exercised it, so four inserts were attempted instead, confirming `nulls not distinct` and the partial `WHERE` are both load-bearing. Three copy defects found and fixed (`youth-b-D1` all-day zone note, `D2` an ambiguous `1/2/2027` on the one screen about unambiguous dates, `D3` "1 events updated"); the fixes changed words only, proved by an identical database afterwards. Still open: **deployed build not opened during this walk** (since walked twice), no real device, dark mode from screenshots, **step 10's zone change not walked** (an OS-level action), the browser zone equalled the ward zone throughout, and **J6 was never answered** — whether the *From a schedule feed* chip reads at a glance. **`c24d52b` reversed a rule this walk ran under** — `/youth` now formats in the ward's zone, not the reader's — which invalidates none of the observed instants but did make scenario 051's step 10, checklist line, Purpose and Notes stale; all four corrected 2026-08-30 |
| 2026-08-28 | youth-coverage-calendar | f52bcaf | best-yet | Scenarios 053 and 054 walked by an AGENT in a browser against the hosted project; user answered the judgement questions, **two of which came back as defects**. Record written 2026-08-30 from the walk evidence — see the `youth-ics-import` row for why. Every `event_type` and coverage state read back with the service client against the stored row, never the seed script. All six badges correct; **the user's rule proved in both directions** by moving a cancelled event and then a `tbd` event into the past, which separates the "cancelled" branch from the "past" one. Zero renders nothing — the strip disappears rather than reading "0". **The `youth-b` guarantee holds in its strongest form**: a row that WAS written by an import kept its hand-corrected `away`. Both manual-entry branches distinguishable in the audit (`classified_from_location` vs `chosen`), which is why `createActivityEventSchema` dropped its `.default("tbd")`. `ward_council_member`'s API refusals walked from that account's own session — no automated test covered that role. **Five copy defects: four from the review, and a fifth INTRODUCED by fixing the fourth and caught only by re-walking** ("Home or away stays Away — this file would have set it to Home or away not set" typechecked and broke no test); now asserted over every combination rather than three spelled-out cases. **One defect found and deliberately NOT fixed**, recorded so a later reader finds a decision rather than an oversight: `listActivityEvents` has no `.order("id")` tiebreaker, so events sharing an instant reorder whenever any is edited — reproduced with a no-op UPDATE. Four checklist lines described states the app cannot reach and were rewritten. Still open: deployed build not opened during this walk, no real device, dark mode from screenshots, `yw-president` not walked. Scenario 053's month-grid zone note was corrected 2026-08-30 — the ward's zone, not the reader's; **the invariant it protects is unchanged, only its premise** |
| 2026-08-29 | youth-followup-feed | 99af99f | best-yet | Scenarios 055 and 056 walked by an AGENT in a browser 2026-08-28 and **both re-walked 2026-08-29** after the ITER-021/022 fixes; user reviewed and answered the judgement questions. Record written 2026-08-30 from the walk evidence — see the `youth-ics-import` row for why. **The privacy boundary was probed from the bishop's own session by four separate routes**, not asserted from a policy reading: he read the shared note and reached neither private note, and `GET .../private-note` answered `200 {"note":null}` rather than 403 — the policy denies the row, so "not yours" and "none yet" are the same answer. Migration 057c's contrast read from four accounts' own sessions: the ward council member sees **every activity and every event, and one follow-up**, which is the ward-wide arm proving the policy's LEFT JOIN. All four flag transitions verified including `flag_sent_at` clearing to null; one recipient, a pointer not a note, and **the recipient cannot open what they were told about** (403 on three routes) — which makes the body rule structural rather than remembered. Wider reads did not widen a write (404 on a cross-org PATCH with visibility ON). `/visits/feed` survived the shared-helper move with `authorLabel` populated, proving the seam in both directions. **Three defects: `ITER-021` (the follow-up control offered on another organization's event — `youth-a-D1`/`visits-d` a THIRD time, in the slice whose own plan quotes it; fixed at BOTH offer sites, since fixing the card alone would have shipped it a fourth), `ITER-022` (colour as the only signal on "Did you go?"), and a harness trigger-key drift that made the central check unverifiable — that one became ITER-023, closed by `b2b8aab`.** Still open: **deployed build not opened in either walk**, no real device, dark mode from screenshots, `yw-president` not walked, and **scenario 055's own follow-up loop was NOT re-walked on 08-29** — that pass borrowed 056's ward, which is a real limitation rather than an equivalent. **Raised by the walk and left as found:** the panel heading counts a row whose button the ownership gate removed, so "Waiting on your follow-up (2)" includes one the reader may not write — newly visible rather than newly introduced, and a product decision rather than a bug to patch mid-walk |
| 2026-08-29 | youth-occasions | 43a10c9 | best-yet | Scenario 059 walked by an AGENT in a browser against the hosted project; user reviewed the walk report and answered four judgement questions. Every write read back with the service client. **Three defects found that 3262 tests missed, all fixed and re-verified**: a `<select>` overflowing at 375px (a flex item's `min-width: auto` refuses to shrink, so `w-full` alone was not enough), "One of these young people" rendered above a group of one, and a picker option missing the event's title — the user settled that last one. The migration 046/047 composite-key regression is proved end to end for the first time: deleting an occasion does not raise and takes no games with it. Still open: **deployed build unopened** though it is pushed and Vercel deploys from `main`, no real device (375px was a resized desktop viewport), dark mode judged from screenshots only, the bishopric path not walked, the two "with the server stopped" failure cases not simulated, and the `away`-not-copied rule proved by route test rather than by a walk |
| 2026-08-29 | youth-activity-scope | b941089 | best-yet | Scenario 049 re-walked by an AGENT in a browser against the hosted project; user answered all five judgement questions. **`youth-a-D1` confirmed FIXED** — Edit and Remove now appear on exactly the rows `canManageActivityProfile()` permits, proved across four accounts and mirrored so it is not an artifact of seed order. 26 machine checks, **0 defects**. The API was probed independently because a hidden button is not a boundary: cross-org PATCH/DELETE → 404, a POST naming another org → 403, and 4 rows read back unchanged with no audit rows for the refused calls. Three checklist corrections, because `youth-e` moved the list to `/youth/profiles`. Still open: **the deployed build is BROKEN on this very page** (see the `deployed-build` row — a rendering defect, not a permission one), no real device, dark mode from screenshots only, the PIN account unwalked, and the event-vs-activity ownership asymmetry is an open product question rather than a defect. Q2's answer became ITER-029 |
| 2026-08-29 | deployed-build | b941089 | **failed** | **First look at `https://wlt-iota.vercel.app`, and it found a bug localhost cannot show.** Event times are server-rendered in the SERVER's zone — UTC on Vercel — so `/youth/profiles` and `/youth/calendar` emit "Sat, Jan 16, 2027, 2:30 AM" where the browser renders "Fri, Jan 15, 2027, 7:30 PM": wrong day, seven hours out, surfacing as React error #418 and as a visible flash before hydration. Invisible in dev because both sides are `America/Denver` there — exactly CLAUDE.md §9's "passes every test on the dev machine and ships wrong". Cause: `toLocaleString(undefined, …)` with no `timeZone` in `EventList.tsx:118` and `ActivityCalendar.tsx:139`; the reader's-zone intent is deliberate, but on a server there is no reader. Likely also `/youth/events/[id]` and the whole visits module (`visitDates.ts:57`), unproven — the harness ward has no appointments. **Also failed:** `/agendas` and `/admin/audit-log` 404 from the bishop's sidebar. **Working:** 22 routes render, auth guards hold, `/public/*` keeps `noindex`, Phase 8's support pills survive the production build. ITER-015 confirmed on a second route. No writes were made to production |
| 2026-08-30 | deployed-build | c24d52b | best-yet | **The timezone fix HOLDS on production, and the row above is superseded** (see `deployed-build-2026-08-30.md`). Proved four ways rather than one: Vercel's UTC server against an `America/Denver` browser, server HTML **string-identical** to the hydrated DOM, expected values computed **outside the app** in Node from the stored `timestamptz`, and a DST case included (the March event resolves UTC−6 where the other three are UTC−7, so a fixed-offset fix would have failed it). Zero `Jan 16, 2027`, no React #418, no overflow at 375/1280, both themes render. `/youth/events/[id]` correct for a future AND a past event, closing one of the previous row's "likely, unproven" guesses. **NEW defect, never previously recorded:** the fixed 56×56 nav button overlays interactive controls at 375px — 12 sampled scroll positions across three pages, including `I'll go`, `Edit`, `Show past events` and a `<select>`; proved by `elementFromPoint`, deliberately NOT by clicking, because pressing `I'll go` writes to production. Mobile-only (hidden at desktop). **Re-confirmed and WIDER than recorded, twice.** The sidebar has **three** unreachable entries, not two: `/agendas` and `/admin/audit-log` 404, and **`/sacrament` answers 307 and silently returns the bishop to `/dashboard`** — the click just does nothing. 15 of 18 destinations answer 200. The cause is one line of design rather than three oversights: `lib/auth/navigation.ts` gates each entry on a **permission the reader holds, never on whether the page exists**, so any phase that ships a permission before its page repeats this. ITER-015 is also wider — **four** routes answer `307 → /login`, not one. **Still unproven and unchanged:** the visits formatter, because the harness ward has 0 appointments/logs/goals so `/visits` renders no dates at all — closing it needs a write. No writes were made to production, and this time it is PROVEN: 0 audit rows in 90 minutes and all seven table counts identical before and after. The harness password was never typed into the page (session minted server-side, handed over loopback, deleted after). **All three judgement items were answered by the user the same day and NONE becomes work:** the 375px overlap is *acceptable*; the three dead links are *expected* until Phases 9/10/11 land; and **no time-zone marker is owed** — "it is to be assumed that it is according to that ward's time zone", which confirms the CLAUDE.md §9 reversal rather than reopening it. Nothing from this walk is waiting on the user |
| 2026-08-30 | youth-manual-entry | c24d52b | best-yet | **Scenario 050 RE-walked** by an AGENT in a browser against the hosted project; user reviewed the walk report and answered four judgement questions the same day (see `youth-manual-entry-2026-08-30.md`). Re-walked because the 2026-08-27 record predates `c24d52b` — this scenario's Notes asserted the reader's-own-zone rule that commit reversed, so step 10 and one review question were testing a rule the app no longer follows. **19 machine checks, 18 passed, 0 console errors.** The bug the scenario exists for is ABSENT: three writes of the same event stored `2027-01-16T02:30:00+00:00` byte-identical, and the DST pair stored an hour apart (`02:30Z`/`01:30Z`) both render 7:30 PM. **`youth-a-D2` confirmed FIXED, both halves** — a new activity reaches the event form at 0 ms and removing one clears its events, neither needing a reload; the two checklist lines recording it as failing were rewritten, which would otherwise have scored a working app as two defects. **Two NEW defects, both backlogged, NEITHER FIXED.** `050-D1` → **ITER-031**: `ActivityProfileList.tsx:317` deletes an activity on one click with no confirm and no undo, cascading to events, sign-ups, follow-ups and the private notes rule 5 calls private forever; fired twice during the walk (3 → 2 events, then 3 → 0) and the audit row records only three ids, so nothing records what was lost — **live in the app today, and the reason this is not `confirmed`**. `050-D2` → **ITER-032, deferred**: the date input still resolves its zone from ambient process state while the card beside it uses the ward's (card `4:30 PM` vs field `19:30`; on create, typed 19:30 → card 4:30 PM), **unreachable today** since every ward and the fallback are `America/Denver`, and deferred on the user's own correct reading. **Two questions closed clean** — cancelled reads as called off, the empty state reads deliberate. **Two became work**, including the user's own idea that a young person who missed an event should leave their statistics entirely (**ITER-030**), which is a real gap: the support percentage already excludes `away`, `cancelled` and `tbd` for the same reason. **Six corrections made to the scenario file**, five of them the app moving on and the checklist not. Still open: **the deployed build not re-checked since 2026-08-30**, no real device (375px was a resized viewport), dark mode from one screenshot, `npm test` NOT run this session, the bishopric path unwalked, and `050-D1`'s deeper cascade hops read from migration 009 rather than reproduced |
